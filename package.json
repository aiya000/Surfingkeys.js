{
  "name": "Surfingkeys.js",
  "description": "My Surfingkeys settings with TypeScript types",
  "private": false,
  "scripts": {
    "// By `|| true` to ignore the parts that can't be helped for now": "",
    "build-surfingkeys-types": "tsc --project tsconfig-surfingkeys.json || true",
    "clean": "rm -rf @types/*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.5.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
