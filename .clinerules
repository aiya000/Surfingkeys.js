# ファイル編集ルール

## ファイルの再読み込み

VSCodeで開いているファイルをVimから編集することが多々あるため、指示を実行する前に対象のファイルを再読み込みすること。
これは特にリファクタリングや変更を行う前に重要です。

```bash
# 例：settings.jsを編集する前に再読み込み
# VSCodeでファイルを再読み込み、または一度閉じて開き直す
```

## ts-check

settings.jsでは`@ts-check`を使用して、型チェックをしていますが、型検査を通す必要はありません。
ヒント程度に活用をしています。
